# Blue Archive Toolkit for Blender

When importing Blue Archive FBX models from Assets Studio into Blender 4.5.6, textures are lost. We fixed this by setting `use_image_search` to `true` and changing the default scale.
